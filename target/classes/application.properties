# ===============================================================
# CONFIGURA��O DO BANCO DE DADOS (DATASOURCE)
# ===============================================================

# URL de conex�o com o seu banco de dados MySQL.
# Troque NOME_DO_SEU_BANCO_DE_DADOS pelo nome real do seu banco (ex: fut360_db).
spring.datasource.url=jdbc:mysql://localhost:3306/NOME_DO_SEU_BANCO_DE_DADOS?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Usu�rio do banco de dados (geralmente 'root' em ambiente de desenvolvimento).
spring.datasource.username=root

# Senha do seu usu�rio do banco de dados.
spring.datasource.password=SUA_SENHA

# ===============================================================
# CONFIGURA��O DO HIBERNATE (JPA)
# ===============================================================

# Permite que o Hibernate (JPA) atualize o banco de dados automaticamente
# com base nas suas classes @Entity. Muito �til para desenvolvimento.
# Op��es comuns: 'validate', 'update', 'create', 'create-drop', 'none'.
spring.jpa.hibernate.ddl-auto=update

# Mostra no console o SQL que o Hibernate est� executando.
# �timo para debugar.
spring.jpa.show-sql=true
